# rvtools

```
make build
make run EXCEL_FILE=path/to/file.xlsx
make clean
```

```
./rvtools -excel-file <path> -db-path <path>
```

If `-db-path` is provided, the database will be persisted to disk.
